# Copy this file to .env and provide values to connect the frontend to a real backend.
# Leaving REACT_APP_API_URL empty keeps the app in mock/demo mode.

# Base URL of the BuddyBoss/WordPress API (e.g., https://gsaps.example.com)
REACT_APP_API_URL=

# Optional media host for assets served outside of the API
REACT_APP_MEDIA_URL=
