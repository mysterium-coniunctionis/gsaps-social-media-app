import React from 'react';
import { Typography, Box } from '@mui/material';

const GroupDetail = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">GroupDetail Page</Typography>
  </Box>
);

export default GroupDetail;

