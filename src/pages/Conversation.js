import React from 'react';
import { Typography, Box } from '@mui/material';

const Conversation = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">Conversation Page</Typography>
  </Box>
);

export default Conversation;

