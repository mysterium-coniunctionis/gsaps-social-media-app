import React from 'react';
import { Typography, Box } from '@mui/material';

const Groups = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">Groups Page</Typography>
  </Box>
);

export default Groups;

