import React from 'react';
import { Typography, Box } from '@mui/material';

const EventDetail = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">EventDetail Page</Typography>
  </Box>
);

export default EventDetail;

