import React from 'react';
import { Typography, Box } from '@mui/material';

const Members = () => (
  <Box sx={{ py: 4 }}>
    <Typography variant="h4">Members Page</Typography>
  </Box>
);

export default Members;

